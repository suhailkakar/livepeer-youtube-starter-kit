import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderColor: "#333",
    borderWidth: 1,
    height: 200,
    alignItems: "center",
    justifyContent: "center",
    borderStyle: "dashed",
    borderRadius: 10,
  },
  text: {
    color: "#fff",
    fontSize: 15,
  },
  video: {
    width: "95%",
    height: "95%",
    borderRadius: 10,
  },
});
